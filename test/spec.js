// var main = require('../index.js');

exports.testRouteAccess = {
  testTwilioAccessesRoute : function(test) {
    test.done();
  },
  testURLCannotAccessTwilioRoute : function(test) {
    test.done();
  }
};

exports.testDataRequest = {
  testOneWordData : function(test) {
    // example: 'array'
    test.done();
  },
  testTwoWordData : function(test) {
    // example: 'linked list'
    test.done();
  },
  testPhraseData : function(test) {
    // example: 'good movie review'
    test.done();
  }
};

exports.testDataParsing = {
  testOneWordData : function(test) {
    // example: 'array'
    test.done();
  },
  testTwoWordData : function(test) {
    // example: 'linked list'
    test.done();
  },
  testPhraseData : function(test) {
    // example: 'good movie review'
    test.done();
  }
};
